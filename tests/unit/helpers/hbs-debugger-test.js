import { hbsDebugger } from '../../../helpers/hbs-debugger';
import { module, test } from 'qunit';

module('Unit | Helper | hbs debugger');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = hbsDebugger(42);
  assert.ok(result);
});
